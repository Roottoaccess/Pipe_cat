# Core dependencies
loguru
python-dotenv
websockets
aiohttp
requests
numpy
scipy
sounddevice

# Async performance (optional but recommended)
uvloop

# Data validation
pydantic

# ML utilities (optional but used by some Pipecat addons)
transformers

# FastAPI (if you expose an API, optional)
fastapi
uvicorn

# Deepgram STT
deepgram-sdk

# OpenAI (LLM + TTS)
openai

# Pipecat Core
pipecat

# Daily Transport (required for create_transport / DailyParams)
daily-python-sdk
